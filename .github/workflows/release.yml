name: Release

on:
  push:
    branches:
      - main
      - next

permissions:
  contents: write
  issues: write
  pull-requests: write
  id-token: write

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run Biome lint
        run: bun run lint

      - name: Run Typecheck
        run: bun run typecheck

      - name: Run tests
        run: bun test tests

      - name: Diagnose secrets
        env:
          NPM_TOKEN_SECRET: ${{ secrets.NPM_TOKEN }}
          NPM_TOKEN_VAR: ${{ vars.NPM_TOKEN }}
          NPM_AUTH_TOKEN_SECRET: ${{ secrets.NPM_AUTH_TOKEN }}
          NPM_AUTH_TOKEN_VAR: ${{ vars.NPM_AUTH_TOKEN }}
        run: |
          echo "NPM_TOKEN_SECRET length: ${#NPM_TOKEN_SECRET}"
          echo "NPM_TOKEN_VAR length: ${#NPM_TOKEN_VAR}"
          echo "NPM_AUTH_TOKEN_SECRET length: ${#NPM_AUTH_TOKEN_SECRET}"
          echo "NPM_AUTH_TOKEN_VAR length: ${#NPM_AUTH_TOKEN_VAR}"

      - name: Run Semantic Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npx semantic-release
